#include<stdio.h>
int main()
{
	int n,b,temp;
	scanf("%d",&n);
	int i,j,count,k,tem,ii,count1,count2;
	for(i=0;i<n-1;i++)
	{
		temp*=10;
	}
	int an[9];
	for(i=count;i<count*10;i++)
	{
		tem=i;
		for(j=0;j<n;j++)
		{
			an[k]=tem%10;
			temp/10;
			k++;
		}
		
		for(ii=0;ii<(n-1)/2;ii++)
		{
			if(an[k]=an[n-1-ii])
				count1++;
			
		}
		if(count1==(n-1)/2)
		count2++;
	}
	b=count2;
	printf("%d",10);
	return 0;
}
